# Cloudflare Pages response headers for adam-young.co.uk.
# GitHub Pages could not set response headers at all — this file is the main
# reason to host on Cloudflare Pages. Publish's .copyResources() copies this to
# Output/_headers, which Cloudflare reads at deploy time.

/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  X-Frame-Options: DENY
  Strict-Transport-Security: max-age=31536000; includeSubDomains
  # No Content-Security-Policy yet: the site still emits inline JSON-LD (and the
  # Cloudflare Web Analytics beacon is injected at the edge), so a strict CSP
  # needs auditing first. Deferred to a follow-up; add one deliberately once tested.

# Static assets change rarely and aren't content-hashed, so a day's cache is a
# safe balance between freshness and edge offload.
/assets/*
  Cache-Control: public, max-age=86400

# styles.css changes with design tweaks; keep it short and revalidating.
/styles.css
  Cache-Control: public, max-age=3600, must-revalidate

# Markdown twins for AI agents (/blog/<slug>.md, /about.md, …). Serve as
# Markdown so agents get the right content type; short cache like the HTML.
/*.md
  Content-Type: text/markdown; charset=utf-8
  Cache-Control: public, max-age=3600, must-revalidate

# llms.txt / llms-full.txt — the llmstxt.org index and full-text dump.
/llms.txt
  Content-Type: text/plain; charset=utf-8
  Cache-Control: public, max-age=3600, must-revalidate
/llms-full.txt
  Content-Type: text/plain; charset=utf-8
  Cache-Control: public, max-age=3600, must-revalidate
